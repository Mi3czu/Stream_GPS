JWT_SECRET=replace-with-a-long-random-secret
# DATABASE_URL is optional locally because compose.yaml provides POSTGRES_PASSWORD.
# DATABASE_URL=postgresql://stream_gps:password@127.0.0.1:5432/stream_gps
